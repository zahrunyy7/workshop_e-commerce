name: workshop7
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

# 1. BARIS INI WAJIB ADA (Sesuai saran error tadi)
environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1

  cupertino_icons: ^1.0.8
  provider: ^6.0.5
  intl: ^0.18.1
  firebase_core: ^2.15.1
  cloud_firestore: ^4.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/profil.JPG